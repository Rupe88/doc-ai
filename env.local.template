# Database
# Get these from your Supabase project settings
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@[YOUR-PROJECT-REF].supabase.co:5432/postgres
DIRECT_URL=postgresql://postgres:[YOUR-PASSWORD]@[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Supabase
# Get these from your Supabase project API settings
NEXT_PUBLIC_SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=[YOUR-ANON-KEY]
SUPABASE_SERVICE_ROLE_KEY=[YOUR-SERVICE-ROLE-KEY]

# GitHub OAuth
# Create a GitHub OAuth App at https://github.com/settings/developers
GITHUB_CLIENT_ID=[YOUR-GITHUB-CLIENT-ID]
GITHUB_CLIENT_SECRET=[YOUR-GITHUB-CLIENT-SECRET]
GITHUB_WEBHOOK_SECRET=[YOUR-WEBHOOK-SECRET]

# AI Provider (Choose ONE - Free options available!)
# The app will auto-detect which API keys you have set

# Option 1: Groq (RECOMMENDED - Fastest & Free!)
# Get free API key: https://console.groq.com/
GROQ_API_KEY=[YOUR-GROQ-API-KEY]
GROQ_MODEL=llama-3.1-70b-versatile

# Option 2: Google Gemini (Free tier: 60 req/min)
# Get free API key: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=[YOUR-GEMINI-API-KEY]
GEMINI_MODEL=gemini-pro

# Option 3: Together AI ($25 free credits)
# Get API key: https://api.together.xyz/
TOGETHER_API_KEY=[YOUR-TOGETHER-API-KEY]
TOGETHER_MODEL=mistralai/Mixtral-8x7B-Instruct-v0.1

# Option 4: OpenAI (Paid - fallback)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=[YOUR-OPENAI-API-KEY]
OPENAI_MODEL=gpt-4-turbo-preview

# Auto-select provider (optional - defaults to groq if available)
AI_PROVIDER=groq

# Qdrant Vector Database
# Sign up at https://cloud.qdrant.io/ or use local instance
QDRANT_URL=https://[YOUR-CLUSTER-ID].qdrant.io
QDRANT_API_KEY=[YOUR-QDRANT-API-KEY]

# Paddle Payment Processing
# Sign up at https://paddle.com and get your API keys
PADDLE_API_KEY=[YOUR-PADDLE-API-KEY]
PADDLE_SANDBOX_API_KEY=[YOUR-PADDLE-SANDBOX-API-KEY]
NEXT_PUBLIC_PADDLE_ENVIRONMENT=sandbox
PADDLE_WEBHOOK_SECRET=[YOUR-PADDLE-WEBHOOK-SECRET]

# Paddle Product IDs (create products in Paddle dashboard)
PADDLE_PRODUCT_ID_PRO=[YOUR-PRO-PRODUCT-ID]
PADDLE_PRODUCT_ID_TEAM=[YOUR-TEAM-PRODUCT-ID]
PADDLE_PRODUCT_ID_ENTERPRISE=[YOUR-ENTERPRISE-PRODUCT-ID]

# Upstash Redis
# Sign up at https://upstash.com/ and create a Redis database
UPSTASH_REDIS_REST_URL=https://[YOUR-REDIS-ENDPOINT].upstash.io
UPSTASH_REDIS_REST_TOKEN=[YOUR-REDIS-TOKEN]

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Encryption Secret (generate a random string for production)
# Use: openssl rand -base64 32
ENCRYPTION_SECRET=change-this-to-a-random-secret-in-production

# Logging
LOG_LEVEL=INFO

